[project]
name = "tw-daily-stock-picker"
version = "0.1.0"
description = "Daily Taiwan stock screener with configurable technical filters."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
tw-stock-picker = "tw_stock_picker.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
